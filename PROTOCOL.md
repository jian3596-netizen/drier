# STM32 与 DWIN 屏通信边界

串口参数：115200、8-N-1。两端目前使用自定义 `FA ... AF` 数据帧，不是标准 DGUS `5A A5` VP 写入协议。

## STM32 → 屏幕

当前为 15 字节：

```text
FA | NTC1电阻(4B) | NTC2电阻(4B) | NTC3电阻(4B) | AF | 0A
```

屏内 T5L51 程序接收电阻值、换算温度，再写入 DGUS VP，因此温度可以自动显示。

## 屏幕 → STM32

当前为 8 字节：

```text
FA | 温度1 | 温度2 | 温度3 | 运行状态 | 设定温度 | 预热标志 | AF
```

## 预热状态同步

单独让 STM32 写 `0x2007` 不会生效，因为现有屏内串口代码没有解析 STM32 发出的标准 DGUS VP 写帧。计划采用现有自定义协议同步实际预热状态：

1. STM32 在预热超过一小时并强制结束时，在 STM32→屏幕的数据帧中反馈实际预热状态。
2. T5L51 解析该状态；状态为结束时清除屏内 `Set_War_Temp`，并把原有预热 VP `0x2006` 更新为关闭。
3. DGUS 页面继续显示 `0x2006`，不再新增 `0x2007`。

此处记录的是两端下一步共同修改的接口约定，当前源码尚未实施该扩帧。
