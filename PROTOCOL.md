# STM32 与 DWIN 屏通信边界

串口参数：115200、8-N-1。两端目前使用自定义 `FA ... AF` 数据帧，不是标准 DGUS `5A A5` VP 写入协议。

## STM32 → 屏幕

温度数据帧为 15 字节：

```text
FA | NTC1电阻(4B) | NTC2电阻(4B) | NTC3电阻(4B) | AF | 0A
```

屏内 T5L51 程序接收电阻值、换算温度，再写入 DGUS VP，因此温度可以自动显示。该帧保持与原屏幕程序兼容。

预热超时命令帧为 5 字节：

```text
FB | 57 | 01 | BF | 0A
```

## 屏幕 → STM32

当前为 8 字节：

```text
FA | 温度1 | 温度2 | 温度3 | 运行状态 | 设定温度 | 预热标志 | AF
```

## 预热超时同步机制

预热超时后，STM32 交替发送温度数据帧和预热超时命令帧。T5L51 收到命令后，将 `Set_War_Temp` 和 DGUS VP `0x2006` 清零，并通过屏幕→STM32 帧返回“预热标志”`0`。STM32 收到确认后停止发送命令。该机制既避免单帧丢失导致状态不同步，也保证温度帧格式与旧屏幕程序兼容。

超时按连续待机时间计算，默认 60 分钟。正式烘干运行期间不累计待机时间；只要中途进入过运行，运行结束回到待机后就从 0 重新计算 60 分钟。
