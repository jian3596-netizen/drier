# STM32 与 DWIN 屏通信边界

串口参数：115200、8-N-1。两端目前使用自定义 `FA ... AF` 数据帧，不是标准 DGUS `5A A5` VP 写入协议。

## STM32 → 屏幕

当前为 16 字节：

```text
FA | NTC1电阻(4B) | NTC2电阻(4B) | NTC3电阻(4B) | 预热强制关闭(1B) | AF | 0A
```

屏内 T5L51 程序接收电阻值、换算温度，再写入 DGUS VP，因此温度可以自动显示。T5L51 同时兼容旧的 15 字节数据帧。

## 屏幕 → STM32

当前为 8 字节：

```text
FA | 温度1 | 温度2 | 温度3 | 运行状态 | 设定温度 | 预热标志 | AF
```

## 预热超时同步机制

STM32→屏幕帧中的“预热强制关闭”字段用于同步超时关停事件：

- `0`：无强制关闭请求。
- `1`：预热已超时关停，T5L51 将 `Set_War_Temp` 和 DGUS VP `0x2006` 清零。

STM32 会持续发送 `1`，直到屏幕→STM32 帧中的“预热标志”返回 `0`。该确认机制可避免因单帧丢失导致控制板已经停止预热、界面仍显示开启。
